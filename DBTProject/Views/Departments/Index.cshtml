@model IEnumerable<DBTProject.Department>

@{
    ViewBag.Title = "Index";

    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table id="departments" class="table table-bordered table-responsive table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Departamento")
            </th>
            <th>
                @Html.DisplayName("Opciones")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.DepartmentName, "Edit", new { id = item.DepartmentID })
                </td>
                <td>
                    @Html.ActionLink("Delete", "Delete", new { id = item.DepartmentID })
                </td>
            </tr>
        }
    </tbody>
</table>
@section scripts
{
    <script>
        /*$(document).ready(function () {
            $("#departments .js-delete").on("click", function () {
                var button = $(this);
                bootbox.confirm("Are you sure you want to delete this customer?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/Departments/Delete/" + button.attr("data-department-id"),
                            method: "POSTS",
                            success: function () {
                                button.parents("tr").remove();
                                console.log("Success");
                            }
                        });
                    }
                });
            });
        });
        */
        $(document).ready(function () {
            $("#departments").DataTable();
        })
    </script>
}